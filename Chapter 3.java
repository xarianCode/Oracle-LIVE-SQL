import java.util.Scanner; 
import javax.swing.JoptionPane;
import java.util.Random;
import java.util.*;


//#region Chapter 3

// one way to create a table, you can use Subqueries, which requires getting data from a pre-existing table.
// CRATE TABLE tablename [(columnname,  ...)]
//  AS (subquery) 


// one example can be formatted as 
//          CREATE TABLE cust_mkt
//            AS (SELECT customer#, city, state, zip, reffered, region
//                FROM customers);


// You can also alter tables via the ALTER TABLE command
//  use ADD to add columns, MODIFY to change a column and using the DROP COLUMN command to drop a column

public static void main(String[] args)
{
    int Value; 
    int subtractor;
    int FinalVal();


    public void FinalVal(){
        return 
    }
        


}

